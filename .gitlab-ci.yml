image: starefossen/ruby-node:latest

deploy:
  script:
    - node --version
    - yarn install
    - yarn build
    - bundle install
    - bundle exec jekyll build
    - yarn build
    - bundle exec jekyll build
    - netlify --dir ./_site --prod --auth $NETLIFY_AUTH_TOKEN
  only:
    - master