image: timbru31/ruby-node:2.7

deploy:
  script:
    - node --version
    - npm install
    - npm build
    - bundle install
    - bundle exec jekyll build
    - npm build
    - bundle exec jekyll build
    - netlify --dir ./_site --prod --auth $NETLIFY_AUTH_TOKEN
  only:
    - master