image: starefossen/ruby-node:latest

deploy:
  script:
    - yarn install
    - yarn build
    - bundle install
    - bundle exec jekyll build
    - node_modules/.bin/surge ./_site
  only:
    - master